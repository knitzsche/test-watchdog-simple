name: test-watchdog
base: core18 
version: '0.1'
summary: Test snapcraft systemd watchdog
description: |
  A test.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: strict

apps:
  watchdog:
    daemon: simple
    command: bin/daemon

parts:
  daemon:
    source: .
    plugin: go
    go-importpath: test-watchdog
    build-packages: [ gcc ]
